env: local

logger:
  level: "info"

server:
  host: "127.0.0.1"
  port: "8080"

storage:
  type: "memory"
  sql:
    dsn: "user=calendar password=calendar dbname=calendar sslmode=disable"

grpc_server:
  host: "localhost"
  port: "50051"

rabbitmq:
  host: "localhost"
  port: "5672"
  username: "guest"
  password: "guest"

scheduler:
  launch_frequency: 5s